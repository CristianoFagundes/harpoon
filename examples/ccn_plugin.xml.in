<harpoon_plugins>

  <!-- 
    DummyPlugin doesn't do anything - just an example of a simple plugin
    -->

  <plugin name="ccnServer" objfile="ccn_plugin.@SHLIB_EXT@" maxthreads="1" personality ="server">
      <active_sessions> 1 </active_sessions>
  </plugin>

  <plugin name="ccnClient" objfile="ccn_plugin.@SHLIB_EXT@" maxthreads="1" personality ="client">
      <active_sessions> 1 </active_sessions>
  </plugin>

</harpoon_plugins>
